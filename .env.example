# VSCode Python 扩展会读取 `.vscode/settings.json` 里配置的 python.envFile。
# 这里仅放非敏感环境变量，便于多设备同步后快速复制为本地 `.env`。
PYTHONUTF8=1
PYTHONIOENCODING=utf-8

# ===== Qlib/研究层（可选）=====
# 说明：该文件仅放非敏感信息；真实密钥仍应放在你自己的 config-private.json 等位置。

# Freqtrade 数据默认位置（与 data/<exchange>/... 对齐）
FREQTRADE_EXCHANGE=okx
FREQTRADE_DATA_DIR=./01_freqtrade/data

# Qlib 数据/模型默认位置（均在 .gitignore 中，可重建）
QLIB_DATA_DIR=./02_qlib_research/qlib_data
QLIB_MODEL_DIR=./02_qlib_research/models/qlib
QLIB_MODEL_VERSION=v1
