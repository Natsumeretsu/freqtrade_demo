# 私钥/个人配置（不要提交到仓库）
/*.env
/.claude/settings.local.json

# 运行配置（允许提交 config*.json，但请勿在其中保存任何密钥）
/config*.local.json
/config*.secrets.json
/config*.private.json
/config-private*.json

# 运行配置（ft_userdir/ 下的私密覆盖：不要提交）
/ft_userdir/config*.local.json
/ft_userdir/config*.secrets.json
/ft_userdir/config*.private.json
/ft_userdir/config-private*.json

# 运行期数据/日志（通常体积较大且可再生成）
# 市场数据（OHLCV/资金费率/盘口等）体积大且可再生成，默认不使用 Git 同步。
# 如确需跨设备复现：建议用脚本重新下载，或使用独立存储（NAS/对象存储）或 Git LFS。
/ft_userdir/data/
/ft_userdir/backtest_results/
/ft_userdir/plot/
/*.sqlite*
/*.log
/*.db-shm
/*.db-wal

# FreqAI 训练产物默认写入 userdir/models/<identifier>/（体积大且可再生成，避免误提交）
/ft_userdir/models/*/

# 外部工具：建议安装到 ~/.codex/tools（避免作为仓库依赖/子模块）
/tools/Wolfram-MCP/

# Python
**/__pycache__/
*.py[cod]
*.bak
.ipynb_checkpoints/
*.so
*.egg
*.egg-info/
dist/
build/

# 测试缓存
.pytest_cache/
.coverage
htmlcov/

# 虚拟环境
.venv/
/.venv/

# 常见系统临时文件
.DS_Store
Thumbs.db

# Windows 设备名误创建（会导致工具扫描异常）
/nul

# Serena（建议同步 memories 与 project.yml，忽略缓存/日志）
/.serena/cache/
/.serena/logs/

# Vibe coding（AI 大脑 / RAG 向量库缓存）
# - In-Memoria：`in-memoria.db` 建议随 Git 同步（跨设备同一个大脑）
# - Local RAG：`.vibe/local-rag/` 为可重建缓存（默认不提交，跨设备时用脚本预热重建即可）
/.vibe/
/.in-memoria/cache/

# freqtrade_docs：离线保存的官方 HTML 原文（体积大，默认不提交；可用脚本下载后再生成 Markdown）
/docs/archive/freqtrade_docs/raw_html/

# wolfram_engine_doc：离线保存的 Wolfram 文档 HTML（体积大，默认不提交）
/docs/archive/other_doc/wolfram_engine_doc/**/raw_html/

# 误创建的策略参考库（根目录下的独立 clone，不是子模块路径 docs/archive/strategies_ref_docs）
/strategies_ref_docs/

# 其它本地缓存/草稿（不作为权威结论层）
/temp/
/in-memoria-vectors.db/
/lancedb/

# 临时数据文件（分析/调试产生的中间文件）
factor_data*.csv
*.db
export_log.txt

# 临时脚本（一次性分析/检查脚本）
check_*.py
analyze_*.py
prepare_*.py
