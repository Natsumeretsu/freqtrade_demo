# 文档编写规范

更新日期：2026-01-17

## 1. 通用原则

**核心理念**：
- 清晰简洁：避免冗长和模糊的表述
- 结构化：使用标题、列表、表格组织内容
- 可操作：提供具体的步骤和示例
- 可维护：包含更新日期和版本信息

---

## 2. Markdown 格式规范

### 2.1 标题层级

**使用规则**：
- H1 (`#`)：文档标题（每个文档只有一个）
- H2 (`##`)：主要章节
- H3 (`###`)：子章节
- H4 (`####`)：细节说明
- 最多使用到 H4，避免过深嵌套

**示例**：
```markdown
# 策略开发指南

## 1. 环境准备

### 1.1 安装依赖

#### Python 环境

...
```

### 2.2 列表格式

**无序列表**：
- 使用 `-` 作为标记
- 缩进使用 2 个空格
- 列表项之间不留空行

**有序列表**：
- 使用 `1.`, `2.`, `3.` 标记
- 子列表使用字母：`a.`, `b.`, `c.`

**示例**：
```markdown
**步骤**：
1. 安装 Python 3.9+
2. 安装依赖包：
   a. 运行 `pip install -r requirements.txt`
   b. 验证安装：`python --version`
3. 配置环境变量
```

### 2.3 代码块

**使用三个反引号**：
- 指定语言：```python, ```bash, ```yaml
- 添加简短说明
- 保持代码简洁（≤20 行）

**示例**：
````markdown
**计算 RSI 示例**：
```python
def calculate_rsi(data, period=14):
    delta = data['close'].diff()
    gain = delta.where(delta > 0, 0)
    loss = -delta.where(delta < 0, 0)
    return 100 - (100 / (1 + gain.rolling(period).mean() / loss.rolling(period).mean()))
```
````

---

## 3. 文档结构

### 3.1 标准结构

**所有文档应包含**：
```markdown
# 文档标题

更新日期：YYYY-MM-DD

## 1. 概述
- 文档目的
- 适用范围
- 前置要求

## 2. 主要内容
...

## 3. 示例
...

## 4. 常见问题
...

## 5. 参考资源
...
```

### 3.2 技术文档结构

**技术报告/评估报告**：
```markdown
# 标题

更新日期：YYYY-MM-DD

## 1. 概览
- 背景
- 目标
- 关键发现

## 2. 详细分析
...

## 3. 结论与建议
...

## 4. 附录
...
```

---

## 4. 内容编写规范

### 4.1 语言风格

**使用中文**：
- 文档正文使用中文
- 代码变量/函数使用英文
- 技术术语保留英文（如 RSI、EMA）

**简洁明了**：
- 使用短句（≤30 字）
- 避免复杂从句
- 一段一个主题

**示例**：
```markdown
✅ 好：RSI 指标用于判断超买超卖。当 RSI > 70 时为超买，RSI < 30 时为超卖。

❌ 差：RSI 指标是一个非常重要的技术指标，它可以用来判断市场是否处于超买或超卖状态，
通常情况下，当 RSI 的数值大于 70 的时候，我们认为市场处于超买状态...
```

### 4.2 表格使用

**适用场景**：
- 对比信息
- 参数说明
- 配置选项

**格式要求**：
- 表头清晰
- 列宽适中
- 对齐一致

**示例**：
```markdown
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `period` | int | 14 | RSI 计算周期 |
| `overbought` | float | 70 | 超买阈值 |
| `oversold` | float | 30 | 超卖阈值 |
```

---

## 5. 特殊元素

### 5.1 提示框

**使用场景**：
- 重要提示
- 警告信息
- 注意事项

**格式**：
```markdown
**⚠️ 重要**：回测结果不代表实盘表现。

**💡 提示**：建议先在模拟环境测试。

**❌ 警告**：不要在生产环境直接运行未测试的策略。
```

### 5.2 链接

**内部链接**：
- 使用相对路径
- 包含文件扩展名

**外部链接**：
- 提供完整 URL
- 添加简短说明

**示例**：
```markdown
详见 [策略开发指南](../knowledge/strategies/crypto_exchange_strategy_deep_dive.md)

参考 [Freqtrade 官方文档](https://www.freqtrade.io/en/stable/)
```

---

## 6. 版本管理

### 6.1 更新日期

**所有文档必须包含更新日期**：
```markdown
更新日期：2026-01-17
```

### 6.2 版本号（可选）

**重要文档使用版本号**：
```markdown
**版本**：v1.0
**创建日期**：2026-01-15
**更新日期**：2026-01-17
```

---

## 7. 检查清单

发布文档前检查：

- [ ] 包含更新日期
- [ ] 标题层级正确（H1 只有一个）
- [ ] 代码块指定了语言
- [ ] 表格格式正确
- [ ] 链接有效
- [ ] 无拼写错误
- [ ] 结构清晰
- [ ] 示例完整可运行

---

## 8. 常见错误

### 8.1 避免的写法

❌ 标题层级跳跃（H1 → H3）
❌ 代码块不指定语言
❌ 表格列不对齐
❌ 链接失效
❌ 缺少更新日期
❌ 内容过于冗长

### 8.2 推荐的写法

✅ 标题层级连续
✅ 代码块指定语言
✅ 表格格式规范
✅ 链接有效且有说明
✅ 包含更新日期和版本
✅ 内容简洁清晰
