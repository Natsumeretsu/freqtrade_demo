# 项目架构文档

**版本**：v2.0 (MVP 架构)
**更新日期**：2026-01-18

---

## 🏗️ 架构概览

本项目采用 **三层分离架构**，聚焦于因子验证而非过早工程化。

```
┌─────────────────────────────────────┐
│ 数据层（02_qlib_research/）         │
├─────────────────────────────────────┤
│ - 数据下载与存储（Parquet 格式）    │
│ - 数据清洗与质量检查                │
│ - 数据预处理管道                    │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│ 研究层（02_qlib_research/notebooks/）│
├─────────────────────────────────────┤
│ - Jupyter Notebook 因子探索         │
│ - IC/t 值计算与验证                 │
│ - 样本外测试                        │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│ 集成层（03_integration/）           │
├─────────────────────────────────────┤
│ - 因子计算模块                      │
│ - 因子验证框架                      │
│ - 数据管道集成                      │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│ 执行层（01_freqtrade/strategies/）  │
├─────────────────────────────────────┤
│ - Freqtrade 策略实现                │
│ - 只使用验证通过的因子              │
│ - 逻辑清晰，易于调试                │
└─────────────────────────────────────┘
```

---

## 📁 目录结构

```
freqtrade_demo/
├── 01_freqtrade/          # 执行层
│   ├── config.json        # 运行配置
│   └── strategies/        # 策略文件
│       ├── base_strategy.py
│       └── SimpleMVPStrategy.py
│
├── 02_qlib_research/      # 数据层 + 研究层
│   ├── data_pipeline/     # 数据管道
│   │   ├── download.py    # 数据下载
│   │   └── clean.py       # 数据清洗
│   └── notebooks/         # 研究笔记
│       └── factor_research/
│           └── 01_funding_rate_factor.ipynb
│
├── 03_integration/        # 集成层
│   ├── simple_factors/    # 因子模块
│   │   └── basic_factors.py
│   ├── factor_validator.py    # 因子验证
│   └── data_pipeline.py       # 数据预处理
│
├── 04_shared/             # 共享配置
│   └── configs/           # 配置模板
│
├── scripts/               # 自动化脚本
│   ├── ft.ps1             # Freqtrade 包装
│   ├── bootstrap.ps1      # 环境初始化
│   └── data/              # 数据脚本
│
├── tests/                 # 测试框架
│   ├── test_factors.py    # 因子测试
│   └── test_data_pipeline.py
│
└── docs/                  # 文档
    ├── QUICKSTART.md
    ├── ARCHITECTURE.md
    └── FACTORS.md
```

---

## 🔄 数据流

```
交易所 API
    ↓
download.py (下载 OHLCV)
    ↓
Parquet 文件 (本地存储)
    ↓
clean.py (数据清洗)
    ↓
Jupyter Notebook (因子研究)
    ↓
basic_factors.py (因子计算)
    ↓
SimpleMVPStrategy (策略执行)
    ↓
Freqtrade (回测/实盘)
```

---

## 🎯 设计原则

### 1. MVP 优先
- 聚焦单一场景（BTC/USDT 15m）
- 避免过度抽象
- 代码简洁（研究阶段 <500 行）

### 2. 假设驱动
- 每个因子都有明确的盈利假设
- 先验证假设，再实现策略

### 3. 增量验证
- 先单因子验证
- 再因子组合
- 最后策略构建

### 4. 样本外测试
- 训练集/验证集/测试集严格分离
- 避免过拟合

---

## 📊 核心模块

### 数据层
**职责**：数据获取、存储、清洗

**关键文件**：
- `download.py` - 从交易所下载 OHLCV 数据
- `clean.py` - 数据清洗（异常值、缺失值）

### 研究层
**职责**：因子探索、验证

**关键文件**：
- `01_funding_rate_factor.ipynb` - 因子研究模板

**验收标准**：
- IC > 0.05
- t 值 > 2
- 样本外测试稳定

### 集成层
**职责**：因子计算、验证框架

**关键文件**：
- `basic_factors.py` - 因子计算函数
- `factor_validator.py` - IC/t 值计算
- `data_pipeline.py` - 数据预处理

### 执行层
**职责**：策略实现、回测、实盘

**关键文件**：
- `SimpleMVPStrategy.py` - MVP 策略模板
- `base_strategy.py` - 策略基类

---

## 🔧 技术栈

- **Python**: 3.11
- **包管理**: uv
- **数据处理**: pandas, numpy
- **因子分析**: alphalens-reloaded, empyrical-reloaded
- **机器学习**: scikit-learn, lightgbm
- **交易框架**: Freqtrade
- **数据存储**: Parquet
- **测试框架**: pytest

---

## 📝 扩展指南

### 添加新因子

1. 在 `basic_factors.py` 中实现因子计算函数
2. 在 Jupyter Notebook 中验证因子有效性
3. 通过验证后，集成到策略中

### 添加新策略

1. 继承 `BaseStrategy` 或 `SimpleMVPStrategy`
2. 实现 `populate_indicators()` 和 `populate_entry_trend()`
3. 编写单元测试
4. 回测验证

---

**最后更新**：2026-01-18
