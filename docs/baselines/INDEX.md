# 策略基线存档索引

本目录存放已验证的策略基线配置，用于后续优化对比和版本回滚。

## 基线列表

### 1. regression_baseline_20260119
**状态**：✅ 已验证
**日期**：2026-01-19
**模型**：LightGBMRegressor（回归）
**收益**：+10.06% (139 笔交易，胜率 98.6%)
**回撤**：-1.33%
**Sharpe**：15.50

**关键突破**：
- 从二分类模型切换到回归模型
- 解决了训练样本不足问题（0.91% → 使用全部数据）
- 实现真正的高频剥头皮（日均 1.54 笔，平均持仓 15.5 小时）

**文件清单**：
- `ETHMicrostructureStrategy.py` - 策略源代码
- `config.json` - FreqAI 配置
- `backtest_regressor.log` - 完整回测日志
- `backtest-result-2026-01-19_20-18-13.zip` - 回测结果数据
- `backtest-result-2026-01-19_20-18-13.meta.json` - 回测元数据
- `README.md` - 详细文档
- `PARAMETERS.md` - 参数速查表
- `check_label_distribution_05.py` - 标签分布分析脚本
- `simulate_entry_conditions.py` - 入场条件模拟脚本
- `analyze_backtest_predictions.py` - 预测分析脚本

**使用方法**：
```bash
# 回测
./scripts/ft.ps1 backtesting \
  --strategy ETHMicrostructureStrategy \
  --freqaimodel LightGBMRegressor \
  --timerange 20240401-20240630

# 查看详细文档
cat docs/baselines/regression_baseline_20260119/README.md

# 查看参数速查
cat docs/baselines/regression_baseline_20260119/PARAMETERS.md
```

---

## 基线管理规范

### 创建新基线
1. 在 `docs/baselines/` 下创建新文件夹，命名格式：`<类型>_baseline_<日期>`
2. 复制以下文件：
   - 策略源代码（`.py`）
   - 配置文件（`config.json`）
   - 回测日志和结果
   - 相关分析脚本
3. 创建 `README.md` 和 `PARAMETERS.md` 文档
4. 更新本索引文件

### 基线命名规范
- `<模型类型>_baseline_<YYYYMMDD>`
- 例如：`regression_baseline_20260119`, `classification_baseline_20260120`

### 基线状态标记
- ✅ **已验证**：回测结果优秀，可作为参考基准
- ⚠️ **待验证**：初步测试通过，需要更多验证
- ❌ **已废弃**：性能不佳或已被更好的基线替代
- 🔬 **实验性**：新想法测试，不保证稳定性

### 版本对比
在优化策略时，应与最新的已验证基线对比：
- 交易笔数变化
- 收益率变化
- 风险指标变化（回撤、Sharpe、Sortino）
- 持仓时间变化

### 回滚操作
如果新优化导致性能下降，可以回滚到基线版本：
```bash
# 复制基线策略
cp docs/baselines/<baseline_name>/ETHMicrostructureStrategy.py \
   ft_userdir/strategies/

# 复制基线配置
cp docs/baselines/<baseline_name>/config.json \
   ft_userdir/

# 重新回测验证
./scripts/ft.ps1 backtesting --strategy ETHMicrostructureStrategy ...
```

---

**最后更新**：2026-01-19
**维护者**：Claude (Sonnet 4.5)
